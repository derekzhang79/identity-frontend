<div class="oauth">
  {{#each oauth.all }}
    <div class="oauth__cta-wrapper">
      <a class="oauth__cta--{{ id }}" id="oauth_cta_{{ id }}" href="{{ url }}">{{ text }}</a>
    </div>
  {{/each }}
</div>
